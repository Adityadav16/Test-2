def get_grade(avg, pass_mark=40):

    if avg >= 90:
        return "A"

    elif avg >= 75:
        return "B"

    elif avg >= pass_mark:
        return "C"

    else:
        return "F"


def calculate_average(*marks):

    return sum(marks) / len(marks)
